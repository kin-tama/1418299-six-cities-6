import React from "react";

const LoadingScreen = () => {
  return (
    <p style={{backgroundColor: `white`,
      height: `900px`,
      fontSize: `160px`,
      color: `black`,
      marginLeft: `100px`
    }} >LOADING</p>
  );
};

export default LoadingScreen;
